server:
  port: 8086
spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:mysql://localhost:3306/retail_inventory_db?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: Deepika@18
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
